{{- $tlsEnabled := .Values.global.tls.enabled }}
{{- $hostname := .Values.global.tls.hostname }}
apiVersion: "networking.k8s.io/v1"
kind: "Ingress"
metadata:
  name: {{ include "authorization.fullname" . }}
  labels: {{- include "authorization.labels" . | nindent 4 }}
  annotations:
    traefik.ingress.kubernetes.io/router.middlewares:
      {{ .Release.Namespace }}-{{ include "authorization.fullname" . }}-strip-prefix@kubernetescrd
    {{- if $tlsEnabled }}
      ,{{ .Release.Namespace }}-{{ include "authorization.fullname" . }}-https-redirect@kubernetescrd
    cert-manager.io/cluster-issuer: "letsencrypt-{{ .Values.global.tls.environment }}"
    {{- end }}
spec:
  rules:
    - http:
        paths:
          - backend:
              service:
                name: {{ include "authorization.fullname" . }}
                port:
                  name: "api"
            path: '/{{ include "authorization.name" . }}'
            pathType: "Prefix"
          - backend:
              service:
                name: {{ include "authorization.fullname" . }}
                port:
                  name: "api"
            path: '/api/{{ include "authorization.name" . }}'
            pathType: "Prefix"
  {{- if and $tlsEnabled }}
  tls:
    - hosts:
        - {{ required "ERROR: global.tls.hostname is required when TLS is enabled" $hostname }}
      secretName: {{ .Values.global.tls.existingCertificatSecret | default (printf "ryax-cert-%s" .Values.global.tls.environment) }}
  {{- end }}
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "authorization.fullname" . }}-strip-prefix
spec:
  stripPrefix:
    prefixes:
      - '/{{ include "authorization.name" . }}'
      - '/api/{{ include "authorization.name" . }}'
---
{{- if $tlsEnabled }}
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: {{ include "authorization.fullname" . }}-https-redirect
spec:
  redirectScheme:
    scheme: https
    permanent: true
{{- end }}
