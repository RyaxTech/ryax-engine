apiVersion: "v1"
kind: "Service"
metadata:
  name: {{ include "authorization.fullname" . }}
  labels: {{- include "authorization.labels" . | nindent 4 }}
spec:
  ports:
    - name: "api"
      port: {{ .Values.apiPort }}
  selector: {{- include "authorization.selectorLabels" . | nindent 4 }}
