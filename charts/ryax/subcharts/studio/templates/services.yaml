apiVersion: "v1"
kind: "Service"
metadata:
  name: {{ include "studio.fullname" . }}
  labels: {{- include "studio.labels" . | nindent 4 }}
spec:
  ports:
    - name: "api"
      port: {{ .Values.apiPort }}
  selector: {{- include "studio.selectorLabels" . | nindent 4 }}
