{{ if .Values.global.ryax.userNamespace }}
apiVersion: "v1"
kind: "Namespace"
metadata:
  name: {{ .Values.global.ryax.userNamespace }}
  labels: {{- include "worker-k8s.labels" . | nindent 4 }}
{{- end }}
