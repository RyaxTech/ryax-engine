{{- if and .Values.global.monitoring.enabled .Values.monitoring.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: {{ include "worker-k8s.fullname" . }}
  labels: {{- include "worker-k8s.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels: {{ include "worker-k8s.selectorLabels" . | nindent 6 }}
  endpoints:
  - port: metrics
{{- end }}
