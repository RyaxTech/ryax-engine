{{- if .Values.config }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "worker-k8s.fullname" . }}-config
  labels: {{- include "worker-k8s.labels" . | nindent 4 }}
data:
  config.yaml: |
{{ tpl (toYaml .Values.config) . | indent 4 }}
{{- end }}
